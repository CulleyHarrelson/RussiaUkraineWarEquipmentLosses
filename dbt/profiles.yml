dbt_equipment_losses:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "localhost"
      user: "{{ env_var('DBT_USER') }}"
      pass: "{{ env_var('DBT_PASS') }}"
      port: 5432
      dbname: "dbt_equipment_losses"
      schema: public
      threads: 4
